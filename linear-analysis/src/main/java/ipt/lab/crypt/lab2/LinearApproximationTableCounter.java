package ipt.lab.crypt.lab2;

import static ipt.lab.crypt.common.heys.HeyConstants.BLOCKS_NUMBER;

public class LinearApproximationTableCounter {

    public static long[][] linearApprox(int[] function) {

        long[][] approx = new long[BLOCKS_NUMBER][];

        for (int a = 0; a < BLOCKS_NUMBER; a++) {
            for (int x = 0; x < BLOCKS_NUMBER; x++) {
                //TODO: and what do we supposed to do now?
            }
        }

        return approx;
    }

}
